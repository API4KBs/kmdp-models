<?xml version="1.0"?>
<catalog xmlns="urn:oasis:names:tc:entity:xmlns:xml:catalog">
@foreach{ ns : entries }
    <public publicId="@{ns.id}"
            uri="@{ns.uri}"/>
    <system systemId="@{ns.id}"
            uri="@{ns.uri}"/>
    <uri name="@{ns.id}"
            uri="@{ns.uri}"/>
@end{}
</catalog>