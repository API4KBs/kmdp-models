package {{intfPackageName}};

import edu.mayo.kmdp.terms.VersionableTerm;
import java.net.URI;
import org.omg.spec.api4kp._1_0.identifiers.NamespaceIdentifier;
import org.omg.spec.api4kp._1_0.identifiers.URIIdentifier;

@javax.xml.bind.annotation.adapters.XmlJavaTypeAdapter( {{seriesName}}.Adapter.class )
@com.fasterxml.jackson.databind.annotation.JsonDeserialize( using = {{seriesName}}.JsonDeserializer.class )
public interface {{intfName}} extends VersionableTerm<{{intfName}}, {{seriesName}}> {

  String schemeName = "{{conceptScheme.label}}";
  String schemeID = "{{conceptSchemeTag}}";

  String seriesIdentifier = "{{seriesNamespace}}";
  URIIdentifier seriesUri = new URIIdentifier()
    .withUri( URI.create( "{{seriesNamespace}}" ) );

  NamespaceIdentifier seriesNamespace = new NamespaceIdentifier()
      .withId(seriesUri.getUri())
      .withLabel(schemeName)
      .withTag(schemeID);

  default URIIdentifier getSeriesUri() {
    return seriesUri;
  }

  @Override
  public NamespaceIdentifier getNamespace();

}
